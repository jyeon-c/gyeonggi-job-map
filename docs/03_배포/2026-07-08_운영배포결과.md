# 2026-07-08 운영 배포 결과

## 배포 대상

- 커밋: `a36adae`
- 브랜치: `main`
- URL: https://jobmapkorea.com
- 방식: EC2에서 `git pull --ff-only origin main` 후 `docker compose up -d --build`

## 검증 결과

| 항목 | 결과 |
|---|---|
| nginx / backend / db | 모두 Up, DB healthy |
| 메인 화면 | HTTPS 200 |
| 채용공고 API | `totalElements=460` |
| DB 시드 | 로그에서 `채용공고 460건 ... 갱신 완료` 확인 |
| 관리자 API 비인증 | HTTP 401 정상 |
| 운영 서버 HEAD | `a36adae` |
| `/api/location` 폴백 | 정상 (`available=false`) |

## 현재 운영 상태

- 사용자 화면과 관리자 화면 모두 운영 배포 완료.
- 사용자 화면은 마감 공고를 제외해 336건을 노출한다.
- 관리자 통계는 전체 460건, 마감 제외 336건, 마감 124건을 표시한다.
- 관리자 공고 등록/수정은 지역·주소 입력 기반으로 좌표를 자동 생성한다.
- 운영 DB 자동 백업은 `~/jobmap-backups`에 매일 생성된다.
