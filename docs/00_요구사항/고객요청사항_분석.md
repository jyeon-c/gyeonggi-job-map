# 고객 요청사항 분석

> 원본: `고객_요청사항.xlsx` (발주처 요청 기능 **10건**). 명세서: `00_실습과제_명세서.md`.
> 상태 기호: ✅ 구현 / 🟡 부분 / ⛔ 미구현

## 프로젝트 개요 (요청서 상단)

- 프로젝트: 일자리 맵(JOBABA MAP) 웹 서비스
- 목적: 특정 웹페이지별 '일자리 맵' 기능 **분양(이식)**
- 배포 방식: **새창(팝업) 방식 — 분양처별 브랜딩 적용**
- 대상 환경: PC / 모바일 웹 모두
- 일정: 8월 테스트, 9월 초·중 오픈

## 요청 기능 10건 · 구현 상태

| No | 요청 기능 | 세부 요청 | 상태 | 구현/비고 |
|---|---|---|---|---|
| 1 | 반응형 팝업 구조 | PC/모바일 팝업 크기 자동 · 분양처 브랜딩(로고) · 상단 헤더 · URL 파라미터로 분양처 구분 | 🟡 | 팝업(embed.js)+반응형·옵션(label/color/pos) ✅ / 분양처 로고·헤더·URL 파라미터 ⛔ |
| 2 | 공공/민간 지도 표시 | 마커 색 구분 · 마커 클릭 상세(PC=좌측/모바일=하단) · 줌/드래그 시 마커 자동 갱신 | ✅ | 색·뱃지 ✅, 상세 미니카드 PC=좌측·모바일=하단 ✅, 줌/드래그 시 마커 자동 갱신 ✅(#9) |
| 3 | 접속 환경별 기본 위치 | PC=IP 기반 · 모바일=GPS 기반 · 최초 접속 위치 안내 | 🟡 | 최초 접속 위치 안내(1회) ✅, 모바일=GPS 사용 제안 ✅, PC 기본=경기 중심(IP 지오코딩은 외부 API 미사용으로 근사) |
| 4 | 현재 위치 기능 | 현재 위치로 이동+주변 표시 · 마커 간략정보 · 오차 안내 | 🟡→✅ | **이번 작업에서 구현**(내 위치 버튼+주변) — 백엔드 nearby API 활용 |
| 5 | 주소 검색 기능 | 장소/주소 검색→지도 이동 · 최근 검색 저장 | ✅ | 카카오 services(주소/장소)로 지도 이동+주변순 목록, 직무 키워드는 목록필터로 자동 구분, 최근검색 저장/삭제 |
| 6 | 채용 목록 섹션 | 지도 표시분 목록 · 카드(기관·제목·직무·마감·거리·공공민간) · 지원 이동 · 마감/거리순 정렬 · 검색필터 아이콘 | 🟡 | 목록·카드·원문이동·마감순·검색필터 ✅ / 거리·거리순·직종 ⛔(거리는 위치기능과 연동) |
| 7 | 검색 / 필터 | 키워드 검색 즉시반영 · 필터(직종·경력·학력·고용형태·희망임금) · 목록클릭 지도이동 · 초기화 | 🟡 | 키워드·경력·학력·고용형태·초기화·목록클릭 지도이동 ✅ / **직종·희망임금 필터 ⛔** |
| 8 | 일자리 정책 정보 연계 | 정책정보 아이콘 · 잡아바 지원정책 4개 · 클릭 시 새창 | ⛔→✅ | **이번 작업에서 구현**(정책 4링크 새창) |
| 9 | 채용공고 데이터 연계·표시 | 지도 범위 조회 · 이동/확대 시 갱신 · 스크롤 추가로딩 · 전체 수 표시 · **마감 공고 미표시** | ✅ | 지도 범위(뷰) 필터 + 이동/확대 시 자동 갱신, 목록 무한스크롤(30씩), 전체 수 표시, 마감 미표시 모두 반영 |
| 10 | 성능 및 보안 (공통) | 캐싱·이미지 최적화 · API 키 도메인 제한 · 위치정보 동의 안내 · XSS 방지 | 🟡→✅ | 도메인 제한 ✅(카카오) · XSS 방지 ✅(esc/DTO) · gzip/CF 캐싱 ✅ · **위치 동의 안내 → 이번 작업 반영** |

> ※ 원본에는 "관리자 통계"가 없음(총 10건). 이전 인계 문서의 "11번 관리자 통계"는 추정이었으나, 보너스로 구현해 `/admin.html` 로 제공 중.

## 좌표 못 구한 공고 처리 — 결정

- **방침: 지역 중심점 대체**(제외 아님). 파이프라인이 시·군 중심점 + 결정적 지터로 근사 좌표를 부여(`geocodePrecision=region_approx`).
- 시·군조차 매칭 안 되는 극소수(약 5건)만 좌표 null → **지도 마커는 생략, 목록에는 표시**.
- 프런트는 `region_approx` 공고에 "위치 근사" 표기.

## 이번 작업 범위 (요청 반영)

- [x] #4 현재 위치 기능 (내 위치 버튼 → 이동 + 주변 일자리, 위치 동의 안내 #10)
- [x] #6 거리순 정렬 · 카드 거리 표시(위치 기준)
- [x] #9 마감된 공고 자동 미표시
- [x] #8 일자리 정책 정보 4개 링크(새창)
- [x] 좌표 처리 방침 확정(지역 중심점 대체)

## 후속 과제 (미구현 — 우선순위)

- #7 직종·희망임금 필터 (직종은 분류 데이터 연계 필요)
- #1 분양처별 브랜딩(로고·헤더·URL 파라미터)
- #3 접속환경별 자동 기본위치(PC=IP / 모바일=GPS 자동)
- #2 PC 상세 좌측 배치 (마커 자동 갱신은 #9로 완료)

## 제약사항 (명세 기준)

- 지도: 카카오맵(JS 키 본인 발급) · 좌표: 주소 지오코딩 파생 · 실 DB 미연동(자체 DB)
- 산출물에 실제 개인정보 미포함(제공 CSV 마스킹, 원본 노출 주의)
