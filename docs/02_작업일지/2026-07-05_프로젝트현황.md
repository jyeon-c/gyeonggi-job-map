# 프로젝트 현황 요약 (2026-07-05 기준, Claude Code 인수인계용)

> 이 문서는 IntelliJ+Claude Code로 작업을 넘길 때 지금까지의 결정사항과 현재 상태를 파악시키기 위한 문서입니다.
> `CLAUDE.md`(프로젝트 루트)의 규칙과 함께 참고하세요.

## 프로젝트 개요

- 이름: **경기도 일자리맵** (레포/폴더명: `gyeonggi-job-map`)
- 성격: 실습 과제 — 지도 기반 채용공고 탐색 웹 서비스, 팝업 위젯 형태로 다른 페이지에 이식
- 참고 완성본: https://jobaba-map.tanauxd.com
- 일정: 8월 테스트, 9월 오픈 목표
- 원본 자료: `고객_요청사항.xlsx`(발주처 요청 11건), `data/` 폴더의 CSV들(채용공고, 공통코드, 직종분류, 기업주소)

## 확정된 기술 스택 / 아키텍처

- **백엔드**: Spring Boot + **Gradle** (기존 프로젝트가 Gradle이라 통일)
- **DB**: PostgreSQL + PostGIS (지도 bbox 조회, 반경 검색, 거리순 정렬에 필요)
- **프런트**: 순수 HTML/JS/jQuery + 카카오맵 SDK (팝업 이식 방식이라 무거운 빌드 도구 없이)
- **배포**: ~~Cloudflare Pages + AWS EC2 분리~~ → **AWS EC2 단일 인스턴스로 통합 결정**
  - EC2 안에 Docker Compose로 nginx(정적 프런트 서빙 + `/api` 리버스 프록시) + backend + db(PostGIS) 세 컨테이너
  - CORS 불필요 (같은 오리진)
  - 도메인은 아직 미구매 — Elastic IP만 먼저 발급받아 EC2에 고정해두는 걸 권장한 상태
- **개발 도구**: IntelliJ + Claude Code로 **백엔드/프런트/인프라 전부 통일** (Cursor, VS Code는 안 씀. Gradle 확장자 연결 체크 완료)
- **Antigravity**: 메인 개발엔 안 쓰고, 나중에 QA/브라우저 검증 단계에서만 보조로 사용 예정
- **디자인**: Figma 에이전트/MCP 검토했으나, 요금(에이전트는 Professional 유료 필요) 및 프로젝트 규모(화면 4~5개) 감안해 **Figma 없이 Claude Code에게 직접 화면 구현을 요청하는 방식으로 결정**. Indeed·잡코리아·원티드 UI 패턴을 참고 레퍼런스로 삼기로 함.

## 문서/버전관리 체계

- `docs/` 폴더 = Obsidian 볼트 (프로젝트 루트 자체를 볼트로 열어야 함, 새 볼트를 별도 하위 폴더로 만들면 안 됨)
  - `00_요구사항/고객요청사항_분석.md` — **아직 빈 템플릿 상태, 엑셀 11건 내용 채워 넣기 필요**
  - `01_설계/ERD.md`, `API명세.md` — 초안 작성됨, 실제 구현하며 업데이트 필요
  - `01_설계/화면설계_Figma연동.md` — Figma 프롬프트 모음(현재는 Figma 안 쓰기로 했으므로 참고용, 필요시 삭제 가능)
  - `02_작업일지/템플릿.md`, `03_배포/인프라구성.md`
- `CLAUDE.md`(루트) — Git 자동 커밋/푸시 규칙, 브랜치 전략(main/dev), 하지 말아야 할 것(원본 CSV 커밋 금지 등) 명시됨
- `.gitignore` 작성 완료 (`data/raw/`, `.env`, `application-local.yml`, `.gradle/`, `.idea/` 등 포함)
- GitHub 레포: `https://github.com/JYeon-C/gyeonggi-job-map` — **push 성공 여부 마지막에 확인 안 됨, Claude Code가 제일 먼저 `git status`/`git log`/`git remote -v`로 현재 상태부터 확인 필요**
- 로컬 초기 세팅 중 파일들이 `docs/` 하위 폴더 없이 루트에 평탄하게 커밋된 이력이 있어, **폴더 구조가 의도대로 정리됐는지도 재확인 필요**

## 다음 작업 순서 (제안)

1. `git status`, `git remote -v`, `git log --oneline`로 현재 레포 상태 점검
2. `docs/` 폴더 구조가 `00_요구사항/01_설계/02_작업일지/03_배포`로 올바르게 되어 있는지 확인, 아니면 정리
3. `고객_요청사항.xlsx` 내용을 읽어서 `docs/00_요구사항/고객요청사항_분석.md`의 요구사항 표 채우기
4. 프런트 메인 화면(지도+리스트+검색, Indeed/잡코리아/원티드 참고)부터 `frontend/`에 구현
5. `docs/01_설계/ERD.md`, `API명세.md` 기준으로 Spring Boot 백엔드(Gradle) 스캐폴딩
6. CSV 지오코딩 배치 스크립트 작성 (`data/데이터_안내.md`의 지오코딩 방침 참고: 고용24는 주소 100%, 잡코리아는 사업자번호 매칭 시 24%만 정확)
7. `docker-compose.yml`(nginx+backend+db) 작성, EC2 배포

## 아직 안 정해진 것

- 요청사항 11번(관리자 통계) 진행 여부
- 좌표 못 구한 공고 처리: 제외 vs 지역 중심점 대체

## 작업 요약 (2026-07-05, Claude Code)

- `dev` 브랜치 생성 후 '다음 작업 순서' 4번 착수: 프런트 메인 화면(검색바+필터 칩 / 좌측 카드 리스트 / 우측 지도 자리)을 `frontend/`에 HTML/CSS/jQuery로 구현.
- 카카오맵은 SDK 자리만 확보하고 목데이터 12건 기반 임시 마커로 카드↔마커 선택 동기화, 검색/필터/정렬/모바일(리스트·지도 토글) 반응형까지 로컬 브라우저에서 동작 확인 완료.
- Node/Python 미설치 환경이라 PowerShell 기반 정적 서버(`.claude/serve.ps1`)를 미리보기용으로 추가.
