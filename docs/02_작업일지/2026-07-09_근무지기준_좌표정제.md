# 2026-07-09 근무지 기준 좌표 정제

- 채용 지도 위치 기준을 기업/본사 좌표가 아닌 공고 근무지 기준으로 재정의했다.
- 고용24는 `BASIC_ADDR + DETAIL_ADDR`, 잡코리아는 상세 주소 부재로 `AREA_INFO` 지역 근사 좌표를 사용하도록 데이터 빌드 로직을 수정했다.
- `기업좌표_샘플.csv`와 `기업주소_사업자번호.csv`는 기업 위치 보조 자료로만 보고 공고 좌표 우선순위에서 제외했다.
- 에듀플렉스 고잔점에 적용했던 `geocode-overrides.json` 수동 보정을 제거했다. 잡코리아 공고는 예외 없이 원본 `AREA_INFO` 기준 지역 근사로 통일한다.
- 지역 근사 좌표의 결정적 지터를 제거했다. 정확 주소가 없는 공고를 임의로 흩뿌리면 산/하천으로 밀릴 수 있어, 시·군 대표 좌표에 고정한다.
- 재생성 결과: 총 460건, 수동 정확 위치 0건, 지역 근사 460건(고용24 27건, 잡코리아 433건), 기업좌표 적용 0건, 중복/결측/형식 오류 0건.
- 정책정보 메뉴 링크를 청년 취업지원(`job.gg.go.kr/jobSprt`), 직업훈련·교육(`job.gg.go.kr/empmEdu`), 창업 지원(`apply.jobaba.net/bsns`), 잡아바 메인(`job.gg.go.kr/main`) 4개로 갱신했다.
- 관리자 통계 API와 화면에 전체 적재 공고, 현재 지도 노출(마감 제외), 마감 제외 공고 수를 추가했다. 기준일은 Asia/Seoul 현재일로 표시한다.
- 사용자 지도 화면 좌측 목록 헤더에 마감 기준일을 Asia/Seoul 현재일 기준으로 표시하고, 정적 리소스 버전을 갱신했다.
- 관리자 페이지에 채용공고 기본 CRUD(검색, 등록, 수정, 삭제)를 추가했다. 운영 DB 변경 보존을 위해 prod 시드 로더는 기본적으로 DB가 비어 있을 때만 `jobs.json`을 적재하도록 조정했다.
- EC2 Docker PostgreSQL/PostGIS 운영 DB 자동 백업 스크립트를 추가했다. `pg_dump` custom format 백업과 SHA-256 체크섬을 `~/jobmap-backups`에 저장하고 14일 보관한다.
- 매일 03:20 실행하는 cron 등록 스크립트와 DB 백업 운영 가이드를 추가했다.
- CSV를 단순 콤마 분리가 아니라 표준 CSV 파서 기준으로 엄격 검증하도록 데이터 빌드 스크립트를 보강했다.
- 실습자료 원본 `데이터_안내.md` 기준에 맞춰 잡코리아 `BIZ_NO` ↔ `기업주소_사업자번호.BIZRNO` 조인으로 주소를 보완하고, 지오코딩 실패 시 `AREA_INFO` 지역 근사로 fallback하도록 다시 매핑했다.
- 재생성 결과: 원본 600건(고용24 100, 잡코리아 500) → 삭제/비활성 67건 및 비경기 고용24 73건 제외 → 최종 460건. 잡코리아 주소 조인 105건, 품질 검증 오류 0건.
- 카카오 REST 키는 환경변수로만 주입하고 파일/커밋에는 남기지 않는 방식으로 주소 지오코딩을 재실행했다. 좌표 정밀도는 `exact` 25건, `company_address` 97건, `region_approx` 338건으로 갱신했다.
- 사용자 지도 미니카드와 관리자 공고 폼에서 `company_address`를 “기업주소 기준”으로 구분 표시하도록 보강했다.
